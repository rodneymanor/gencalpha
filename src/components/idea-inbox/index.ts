// Idea Inbox Component Exports - Alias to content-inbox during migration
// Phase 1: Component alias re-exporting everything from content-inbox

// Re-export everything from content-inbox
export * from "../content-inbox";

// For future: When ready to migrate, we can start importing from here
// and gradually move components over without breaking existing imports
